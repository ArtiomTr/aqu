{
    "name": "example",
    "version": "0.0.0",
    "private": true,
    "dependencies": {
        "${name}": "file:.."
    },
    "scripts": {
        "start": "trwl build && node ./dist/index.js",
        "prepublishOnly": "echo \"This package is an example. It is not intended to be published.\" && exit 1"
    },
    "devDependencies": {
        "trwl": "file:../node_modules/trwl"
    }
}
