/**
 * @type {import('../../src/typings').TemplateScript}
 */
const script = {
    initialize: () => ({
        extend: "_base",
        templateFilePaths: ["./example/src/index.js"],
    }),
};

export default script;
